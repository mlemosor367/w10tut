The application has 3 pages.
1. index.hmtl with link to upload and diplay the data
2. upload.html to upload a cvs file with the expected format of Weather_Data.csv
The upload files are keept on static/files

3. display.html to show all records stored in the sqlLite DB

The sqlListe databe called Weather.db located on the root

the app.py containts the logic to:
show the webpages 
show the counter
create db
Storage data into the weather db